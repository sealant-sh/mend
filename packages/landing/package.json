{
  "name": "@mend/landing",
  "version": "0.0.0",
  "private": true,
  "description": "Landing (docs/adr/0007-landing.md): the land orchestration, the pull request step through a workspace's gh, and the pull request description",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "oxlint . --no-error-on-unmatched-pattern",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mend/db": "workspace:*",
    "@mend/domain": "workspace:*",
    "@mend/sealant": "workspace:*",
    "@mend/sessions": "workspace:*",
    "@mend/store": "workspace:*",
    "@sealant/sdk": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@mend/typescript": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
