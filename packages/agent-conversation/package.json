{
  "name": "@mend/agent-conversation",
  "version": "0.0.0",
  "private": true,
  "description": "How a protocol-mode session reads as a conversation: turns, items and requests, platform-free",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "oxlint . --no-error-on-unmatched-pattern",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsgo -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@mend/typescript": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
