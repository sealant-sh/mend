---
"@sealant/mend": minor
---

The multi mode gate is computed. Each item answers from this build, this instance's configuration,
or the platform work it waits on; `MEND_TENANCY=multi` refuses to start while any is open and names
each with its fix. `/health` reports whether the gate passes and which items are open, and
`mend operator gate` shows the details.
