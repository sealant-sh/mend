apiVersion: v2
name: mend
description:
  Mend on Kubernetes — web + one API/engine Pod over the capture store (an S3 bucket — Rook RGW or
  Garage — plus Postgres), with the cluster-internal session channel for workspaces.
type: application
version: 0.2.0
appVersion: "0.27.5"
sources:
  - https://github.com/sealant-sh/mend
