# Convenience entry point for the contract used by `mend server setup`.
# Use setup for volume ownership, secret generation, and saved settings.
# Manual Compose use must pre-create store/control volumes and supply a private env file.
name: mend
include:
  - path: deploy/docker/compose.v2.yaml
